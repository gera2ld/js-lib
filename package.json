{
  "name": "js-lib",
  "version": "1.0.0",
  "description": "JavaScript utilities for use in the browser, served on IPFS.",
  "scripts": {
    "build": "esbuild src/*.ts --outdir=dist --minify",
    "format": "prettier --ignore-path .gitignore --write src"
  },
  "keywords": [],
  "author": "Gerald <gera2ld@live.com>",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.6",
    "prettier": "^2.8.4"
  }
}
